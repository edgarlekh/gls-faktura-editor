{
  "name": "gls-faktura-editor",
  "version": "0.1.0",
  "private": true,
  "description": "Client-side редактор ежемесячных фактур GLS (только HTML/CSS/JS, GitHub Pages, без бэкенда)",
  "type": "module",
  "scripts": {
    "test": "node test/recalc.test.js && node test/ui-scenarios.test.js && node test/pdf-parser.test.js && node test/salary-calc.test.js && node test/ai-ops.test.js"
  },
  "devDependencies": {
    "pdfjs-dist": "^4.10.38"
  }
}
